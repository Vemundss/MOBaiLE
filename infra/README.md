# Infra (Optional)

Use this folder when you are ready to define runtime infrastructure.

- `docker/`: local/dev container definitions and compose files.
- `terraform/`: cloud infrastructure as code (networking, compute, secrets, etc).

For early local development, this folder can remain mostly empty.
